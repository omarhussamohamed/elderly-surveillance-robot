╔══════════════════════════════════════════════════════════════╗
║                                                              ║
║     🔧  ESP32 FIRMWARE UPDATE CHECKLIST  🔧                 ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝

CRITICAL: You MUST upload new firmware to ESP32 before testing!

┌──────────────────────────────────────────────────────────────┐
│  WHAT CHANGED IN FIRMWARE                                    │
└──────────────────────────────────────────────────────────────┘

File: firmware/elderly_bot_esp32_wifi.ino

Line ~78-81:
OLD:
  const float WHEEL_RADIUS = 0.0194;
  const int TICKS_PER_REV = 990;

NEW:
  const float WHEEL_RADIUS = 0.0325;
  const int TICKS_PER_REV = 4900;

Line ~234:
OLD:
  float ticks_to_m = (2.0 * M_PI * WHEEL_RADIUS) / TICKS_PER_REV;

NEW:
  float ticks_to_m = (2.0 * M_PI * WHEEL_RADIUS) / (float)TICKS_PER_REV;

┌──────────────────────────────────────────────────────────────┐
│  ARDUINO IDE UPLOAD STEPS (Windows)                         │
└──────────────────────────────────────────────────────────────┘

1. Open Arduino IDE

2. File → Open:
   C:\Users\omarh\Desktop\Graduation Project\
   Elderly_Robot_Project\catkin_ws\src\elderly_bot\
   firmware\elderly_bot_esp32_wifi.ino

3. Verify changes (Ctrl+F to search):
   Search: "WHEEL_RADIUS"
   ✓ Should show: 0.0325 (not 0.0194)
   
   Search: "TICKS_PER_REV"
   ✓ Should show: 4900 (not 990)
   
   Search: "(float)TICKS_PER_REV"
   ✓ Should find the cast

4. Tools → Board → ESP32 Dev Module

5. Tools → Port → Select your ESP32 COM port
   (Usually COM3, COM4, or similar)

6. Click Upload (➜ button) or Ctrl+U

7. Wait for "Hard resetting via RTS pin..." message

8. ESP32 will restart automatically

┌──────────────────────────────────────────────────────────────┐
│  VERIFICATION AFTER UPLOAD                                   │
└──────────────────────────────────────────────────────────────┘

1. ESP32 should connect to WiFi automatically
   (ShellBack network)

2. Check serial monitor (115200 baud) - should see:
   - "WiFi connected"
   - "IP address: 192.168.1.xxx"
   - "Connected to ROS server: 192.168.1.29:11411"

3. On Jetson, check topic:
   rostopic echo /wheel_odom -n 1
   
   Should see odometry data publishing

┌──────────────────────────────────────────────────────────────┐
│  QUICK DISTANCE TEST                                         │
└──────────────────────────────────────────────────────────────┘

After upload, test immediately:

1. Mark robot's starting position

2. SSH to Jetson:
   timeout 10 rostopic pub -r 10 /cmd_vel geometry_msgs/Twist \
     "linear: {x: 0.1, y: 0, z: 0}"

3. Measure distance traveled

   Expected: ~1.0 meter
   If you get ~6cm → firmware NOT updated correctly
   If you get ~1m → SUCCESS! ✅

┌──────────────────────────────────────────────────────────────┐
│  TROUBLESHOOTING                                             │
└──────────────────────────────────────────────────────────────┘

❌ "Upload Failed" error:
   → Check USB cable connection
   → Try different COM port
   → Press and hold BOOT button during upload

❌ ESP32 not connecting to WiFi:
   → Check SSID/password in code (line ~40-41)
   → Verify WiFi is 2.4GHz (ESP32 doesn't support 5GHz)

❌ rosserial not connecting:
   → Check Jetson IP in code: 192.168.1.29 (line ~43)
   → Verify rosserial running: rosnode list | grep serial
   → Check firewall: sudo ufw allow 11411/tcp

❌ Still moving wrong distance:
   → Verify code changes with Ctrl+F search
   → Check you uploaded to correct ESP32 board
   → Restart ESP32 after upload (press EN button)

┌──────────────────────────────────────────────────────────────┐
│  WHY THIS FIX IS NECESSARY                                   │
└──────────────────────────────────────────────────────────────┘

The old calibration (WHEEL_RADIUS=0.0194m, TICKS_PER_REV=990)
was measuring the wheel under load, causing:

  Commanded 1.0m → Robot moves 0.06m (6cm!)

The new kinematic specification uses:
  - Actual wheel diameter: 65mm → radius 32.5mm
  - Proper tick count: 4900 (accounting for gear ratio)
  
Result:
  Commanded 1.0m → Robot moves 1.0m ± 5cm ✅

┌──────────────────────────────────────────────────────────────┐
│  AFTER FIRMWARE UPDATE                                       │
└──────────────────────────────────────────────────────────────┘

Once firmware is uploaded and distance test passes:

1. Run full Boss Level validation:
   bash ~/catkin_ws/src/elderly_bot/scripts/boss_level_test.sh

2. Autonomous square test should now complete properly

3. Map will be 1:1 scale with real world

4. Navigation commands will be accurate

╔══════════════════════════════════════════════════════════════╗
║                                                              ║
║  🎯 UPLOAD FIRMWARE FIRST, THEN RUN TESTS! 🎯              ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝
